<query name="selectOne">
	select ID, USERNAME
	from USER
	where USERNAME = ?
		and PASSWORD = ?
</query>

<query name="checkUser">
	select count(*)
	from USER
	where ID = ?
		and USERNAME = ?
</query>
